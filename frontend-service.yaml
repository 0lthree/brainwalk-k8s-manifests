apiVersion: v1
kind: Service
metadata:
  name: brainwalk-frontend-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: brainwalk-frontend
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30082