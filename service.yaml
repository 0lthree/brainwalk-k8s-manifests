apiVersion: v1
kind: Service
metadata:
  name: brainwalk-backend-service
  namespace: default
spec:
  type: NodePort
  selector:
    app: brainwalk-backend
  ports:
    - port: 8080          # 클러스터 내부 포트
      targetPort: 8080    # 컨테이너 포트
      nodePort: 30081     # 외부에서 접속할 포트